# Vendored LatticeDB wasm (regenerated by scripts/gen-lattice.mjs, sha256-pinned)
static/lattice/
# Generator download cache (offline-capable rebuilds; never tracked)
scripts/.cache/
# Playwright output
playwright-report/
test-results/
# Raw media: the recordings and the generated video takes the film is built
# from, at full quality. They stay out of git; static/media holds the encodes.
.media-raw/
media-brief/takes/*.mp4
