# Local secrets for `wrangler dev`. Copy .dev.vars.example and fill it in.
.dev.vars
.dev.vars.*
!.dev.vars.example
# The knowledge base, written by scripts/prime/corpus.mjs and uploaded to KV.
# The partner tier is built from documents that are not in this repository.
corpus/
# wrangler's own state
.wrangler/
node_modules/
