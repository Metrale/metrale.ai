// SPDX-License-Identifier: AGPL-3.0-only
// Prerendered in its list state. The scene query string never reaches the
// prerenderer; the page reads it on the client.
export const prerender = true;
